

const challengeHandler = (card, turnStage, successFailure) => {

    if (card.hasEffect) {


        // Divide effects into turnStages

        // Divide turnStages into effect types

        // Process effects

        // Side effecting






    }
}

export default challengeHandler